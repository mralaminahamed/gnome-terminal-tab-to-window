<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.terminal-tab-to-window"
          path="/org/gnome/shell/extensions/terminal-tab-to-window/">

    <key name="move-terminal-tab-shortcut" type="as">
      <default>["&lt;Super&gt;&lt;Shift&gt;w"]</default>
      <summary>Move terminal tab to new window</summary>
      <description>
        Global keyboard shortcut to detach the active terminal tab into its own
        window. Press while a supported terminal is the focused application.
      </description>
    </key>

    <key name="debug-logging" type="b">
      <default>false</default>
      <summary>Enable verbose logging</summary>
      <description>
        When on, the extension logs detailed diagnostics to the GNOME Shell
        journal. Leave off for normal use.
      </description>
    </key>

  </schema>
</schemalist>
